type UntitledModel @model @auth(rules: [{allow: public}]) {
  id: ID!
}

type ShamikhQuraan @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  dima: String
}
 